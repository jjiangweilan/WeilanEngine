 You need to fix remaining C1083 include path errors after CMake changes.

     **Context:**
     - CMake include root changed to: `Source/` (was `Source/Engine/`)
     - All includes now need explicit `Engine/` or `Editor/` prefixes
     - Previous session fixed most files but ~100 errors remain

     **Requirements:**
     1. DO NOT touch files in `Source/Engine/ThirdParty/`
     2. DO NOT touch `Source/Engine/Library/Internal/uuids/`
     3. Follow these include rules:
        - Engine files: use `Engine/` prefix (e.g., `#include "Engine/Core/Object.hpp"`)
        - Editor files: use `Editor/` prefix (e.g., `#include "Editor/EditorState.hpp"`)
        - Cross-module: Engine→Editor needs `Editor/`, Editor→Engine needs `Engine/`

     **What to do:**
     1. Run build: `cmake --build ./build -t WeilanEngineEditor --config RelWithDebInfo`
     2. Check C1083 errors and identify patterns
     3. Fix remaining includes systematically:
        - Look for includes missing `Engine/` prefix
        - Look for includes missing `Editor/` prefix
        - Fix angle bracket includes `<Core/...>` → `"Engine/Core/..."`
        - Fix Gizmos references → `"Editor/Gizmos/..."`
     4. Verify no C1083 errors remain

     **Known remaining issues:**
     - Some Gizmos references
     - Possibly locked files that need retry
     - Angle bracket vs quote inconsistencies
